## Setup

```bash
pip install -r requirements.txt
```

Create `.env`:
```
OPENAI_API_KEY=your_key_here
```

## Run

```bash
python agent.py
```

## Usage

Hold **SPACE** to speak, release to send. The assistant remembers facts about you between sessions. `Ctrl+C` to quit.